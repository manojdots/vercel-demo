import Layout from "@/components/global/Layout";
import { Button, Typography } from "@mui/material";

export default function Page() {
  return (
    <Layout>
      <Typography variant="h1">Hello, Buy Page!</Typography>
      <Button variant="contained">hello</Button>
    </Layout>
  );
}
